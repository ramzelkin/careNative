import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    backgroundColor: '#000',
    color: 'yellow',
    padding: 10,
    textAlign: 'center',
  },
  container: {
    height: 100,
    width: '100%',
    borderWidth: 1,
    borderStyle: 'solid',
    borderColor: 'red',
  },
});

export default styles;
